{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="title">
    <h1>Inscription organisateur</h1>
    </div>
    {{ include('user/_form.html.twig') }}
<div class="btn-back m-lg-3">
    <a class="btn btn-primary" {{ path('user_index') }}>back to list</a>
</div>
{% endblock %}
